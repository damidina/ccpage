<div class="spacing--4 x {{ page.id }}">
  <article class="page-wrapper spacing--5">
    <h1 class="page-title font--heading text-align--center {{ page.id }}">{{ page.title }}</h1>
    <div class="spacer spacing--4y"></div>
    <div class="rte-content content">
      {{ page.content }}
    </div>
  </article>
</div>
